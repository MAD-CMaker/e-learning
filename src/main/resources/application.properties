# Define a porta do servidor (opcional, padrão é 8080)
# server.port=8080

# Context Path (opcional, se você quiser que sua aplicação rode em /minhaapp, por exemplo)
# server.servlet.context-path=/ensinoremoto

# Configurações do Thymeleaf (geralmente os padrões funcionam bem)
#spring.thymeleaf.cache=false # Desabilitar cache do Thymeleaf durante o desenvolvimento para ver alterações HTML imediatamente
spring.thymeleaf.prefix=classpath:/templates/
spring.thymeleaf.suffix=.html
spring.thymeleaf.mode=HTML

# Configurações do Banco de Dados MySQL (Spring Boot pode tentar autoconfigurar se encontrar o driver)
# Você já tem o DatabaseConnector.java, mas para usar o DataSource do Spring, você configuraria aqui:
 spring.datasource.url=jdbc:mysql://localhost:3306/ensinoremoto_tcc?useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true
 spring.datasource.username=leonardo
 spring.datasource.password=leonardo
 spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# Para JPA (se for usar no futuro)
# spring.jpa.hibernate.ddl-auto=update # ou validate, none
# spring.jpa.show-sql=true